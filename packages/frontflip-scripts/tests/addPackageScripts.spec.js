let addPackageScripts = require('../scripts/addPackageScripts');

const chai = require('chai');

const should = chai.should();

describe('Get Add Package Scripts', ()=>{
    it('It should return a map of folder paths', (done)=>{
        // addPackageScripts()
        
        done()
    })  
})