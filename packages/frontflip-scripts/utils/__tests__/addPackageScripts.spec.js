let addPackageScripts = require('../addPackageScripts');

const chai = require('chai');

const should = chai.should();

describe('utils - addPackageScripts', ()=>{
    it('It should return a json object', (done)=>{
        // addPackageScripts()
        
        done()
    })  
});