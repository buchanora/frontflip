export const {{#if name}}{{name}}{{/if}}{{#else}}Onboard{{/else}} = (state =  {}, action) => {
    switch (action.type) {
        case 'ACTION_TYPE':
            return 
        default:
            return state
    }
}